const formatters = require('./formatters.js');
const recipients = require('./recipients.js');
const postalAddress = require('./postal-address.js');

module.exports = {formatters, recipients, postalAddress};
